import React, { Component } from "react";

class Filter extends Component {
  //     render () {
  // let filterBook = this.state.books.booktype;
  // if (isEbook === true);
  // return filterBook = this.state.books && `${value.ebook}`;
  // }
  // else {
  // filterBook = <SearchResult />
  // };
  // }
  // render (){
  //     let forSale = ()
  // }
  // }
}
export default Filter;

//saleability
//isEbook

// if 'NOT_FOR_SALE' === true;
// $("form").show('not for sale')

// // if (this.props.books.filtered someway) {
// //         fetch(`https://www.googleapis.com/books/v1/volumes?q=${this.state.title}&filter=${this.state.book.someway}&printType=${printtype}`)
